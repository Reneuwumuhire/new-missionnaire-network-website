PUBLIC_MAIN_URL="http://localhost:8080/"
PUBLIC_LIVE_STREAM_URL=""
LIVE_AUDIO_SOURCE_URL="http://localhost:8000/radio.mp3"
MONGODB_URI=""
YOUTUBE_API_KEY=""
PROJECT_ID="missionnairenetwork"
PUBLIC_VAPID_KEY=""
VAPID_PRIVATE_KEY=""

# Shared secret for the admin app's go-live ping to the main-site internal
# broadcast-event endpoint. Must match `INTERNAL_API_SECRET` in admin/.env.local.
INTERNAL_API_SECRET=""

# Vercel injects this automatically on cron invocations of /api/cron/radio-probe.
# Set it locally to test the cron endpoint with `curl -H "Authorization: Bearer …"`.
CRON_SECRET=""